# NAFDAC Cross-Check for Diet Plan Foods & Supplements\n\nAdd a second source-grounding layer (alongside USDA) that verifies recommended items against the **NAFDAC Greenbook** — Nigeria's official registry of 9,000+ registered drugs, supplements, and packaged foods.\n\n## What I confirmed during exploration\n\n- `https://greenbook.nafdac.gov.ng/` exposes a working **public JSON endpoint** (DataTables server-side). A GET to the root with `?draw=1&start=0&length=5&search[value]=<term>` returns JSON with `recordsFiltered` and a `data[]` of products including `product_name`, `NAFDAC` (registration number), `applicant.name`, `approval_date`, `expiry_date`, `status` (\